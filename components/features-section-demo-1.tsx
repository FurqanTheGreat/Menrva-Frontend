import React from "react";
import { useId } from "react";

export default function FeaturesSection() {
  return (
    <div className="">
      <div className="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-10 md:gap-2 max-w-7xl mx-auto">
        {grid.map((feature) => (
          <div
            key={feature.title}
            className="relative bg-gradient-to-b from-neutral-900 to-neutral-950 p-6 rounded-3xl overflow-hidden"
          >
            <Grid size={20} />
            <p className="text-base font-bold text-white relative z-20">
              {feature.title}
            </p>
            <p className="text-neutral-400 mt-4 text-base font-normal relative z-20">
              {feature.description}
            </p>
          </div>
        ))}
      </div>
    </div>
  );
}

const grid = [
  {
    title: "Personalized Learning",
    description:
      "Menrva adapts to each student's learning style and progress, offering tailored explanations and resources.",
  },
  {
    title: "Real-time Q&A Assistance",
    description:
      "Students can ask questions at any time and receive instant, accurate responses to help with homework, assignments, or concepts.",
  },
  {
    title: "Multilingual Support",
    description:
      "Menrva can understand and respond in multiple languages, making learning accessible to a wider audience.",
  },
  {
    title: "AI-Powered Tutoring",
    description:
      "Leveraging artificial intelligence, Menrva provides accurate, context-aware answers, improving with each interaction.",
  },
  {
    title: "Engaging User Interface",
    description:
      "Menrva offers a user-friendly, interactive interface with easy navigation and visually engaging elements.",
  },
  {
    title: "24/7 Availability",
    description:
      "Menrva is available anytime, allowing students to get help whenever they need it, without waiting for a tutor.",
  },
  {
    title: "Instant Feedback",
    description:
      "Menrva provides immediate feedback on student responses, helping to correct mistakes and improve understanding quickly.",
  },
  {
    title: "Wide Range of Subjects",
    description:
      "Menrva covers multiple subjects like Mathematics, Science, History, Language Arts, and more, supporting students in their academic journey.",
  },
];

export const Grid = ({
  pattern,
  size,
}: {
  pattern?: number[][];
  size?: number;
}) => {
  const p = pattern ?? [
    [Math.floor(Math.random() * 4) + 7, Math.floor(Math.random() * 6) + 1],
    [Math.floor(Math.random() * 4) + 7, Math.floor(Math.random() * 6) + 1],
    [Math.floor(Math.random() * 4) + 7, Math.floor(Math.random() * 6) + 1],
    [Math.floor(Math.random() * 4) + 7, Math.floor(Math.random() * 6) + 1],
    [Math.floor(Math.random() * 4) + 7, Math.floor(Math.random() * 6) + 1],
  ];
  return (
    <div className="pointer-events-none absolute left-1/2 top-0  -ml-20 -mt-2 h-full w-full [mask-image:linear-gradient(white,transparent)]">
      <div className="absolute inset-0 bg-gradient-to-r  [mask-image:radial-gradient(farthest-side_at_top,white,transparent)] from-zinc-900/30 to-zinc-900/30 opacity-100">
        <GridPattern
          width={size ?? 20}
          height={size ?? 20}
          x="-12"
          y="4"
          squares={p}
          className="absolute inset-0 h-full w-full  mix-blend-overlay fill-white/10 stroke-white/10 "
        />
      </div>
    </div>
  );
};

export function GridPattern({ width, height, x, y, squares, ...props }: any) {
  const patternId = useId();

  return (
    <svg aria-hidden="true" {...props}>
      <defs>
        <pattern
          id={patternId}
          width={width}
          height={height}
          patternUnits="userSpaceOnUse"
          x={x}
          y={y}
        >
          <path d={`M.5 ${height}V.5H${width}`} fill="none" />
        </pattern>
      </defs>
      <rect
        width="100%"
        height="100%"
        strokeWidth={0}
        fill={`url(#${patternId})`}
      />
      {squares && (
        <svg x={x} y={y} className="overflow-visible">
          {squares.map(([x, y]: any) => (
            <rect
              strokeWidth="0"
              key={`${x}-${y}`}
              width={width + 1}
              height={height + 1}
              x={x * width}
              y={y * height}
            />
          ))}
        </svg>
      )}
    </svg>
  );
}
